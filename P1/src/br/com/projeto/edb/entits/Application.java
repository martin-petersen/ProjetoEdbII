package br.com.projeto.edb.entits;

import java.io.*;
import java.util.HashMap;
import java.util.Iterator;
import java.util.Map;

public class Application {
    /*
    private Node root;

    public Application() {
        root = null;
    }

    @Override
    public void compress(String fileName) throws IOException {
        HashMap<Character, Integer> teste = new HashMap<Character, Integer> (this.lettersFrequency(new String(fileName)));
        Heap teste_heap = new Heap();
        Iterator it = teste.entrySet().iterator();

        while(it.hasNext()){
            Map.Entry mapElement = (Map.Entry)it.next();
            teste_heap.insert(new Node(mapElement.getKey(), mapElement.getValue() ));
        }

        root = teste_heap.createBinaryTree();


    }

    @Override
    public void decode() {

    }

}


     */
}